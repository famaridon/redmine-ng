import { SiAutoSizeDirective } from './auto-size.directive';

describe('AutosizeDirective', () => {
  it('should create an instance', () => {
    const directive = new SiAutoSizeDirective();
    expect(directive).toBeTruthy();
  });
});
