import { SiAutoFocusDirective } from './autofocus.directive';

describe('SiAutoFocusDirective', () => {
  it('should create an instance', () => {
    const directive = new SiAutoFocusDirective();
    expect(directive).toBeTruthy();
  });
});
