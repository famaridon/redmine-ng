import { TtToggleRowDirective } from './tt-toggle-row.directive';

describe('TtToggleRowDirective', () => {
  it('should create an instance', () => {
    const directive = new TtToggleRowDirective();
    expect(directive).toBeTruthy();
  });
});
